package com.MMI.OUTPOST.config;

/**
*
* @author Sameer Sharma
*/

/*@Component
public class CustomFilter extends GenericFilterBean {
 
    @Override
    public void doFilter(
      ServletRequest request, 
      ServletResponse response,
      FilterChain chain) throws IOException, ServletException {
    	//System.out.println("Hurreyyyyy");
    	if(request instanceof HttpServletRequest) {
    		//System.out.println(((HttpServletRequest) request).getRequestURL());
    	}
    	String password = ((ServletRequest) request).getParameter("api");
        chain.doFilter(request, response);
       // System.out.println("After executing Other Filters");
        boolean isAuthn = SecurityContextHolder.getContext().getAuthentication().isAuthenticated();
       // System.out.println(isAuthn+">>>>>>>>>>>>>>");
    }
}*/